---
dbms:
  - driver: sqlserver
    endpoints:
      - name: us-sqlserver-prod-ha
        dsn: "sqlserver://sa:Password&1@localhost"
      - name: us-sqlserver-test
        dsn: "sqlserver://sa:Password&1@localhost"
    operations:
      # missing create
      delete:
        name: "sp_delete"
        inputs:
          k8sName: k8sName
