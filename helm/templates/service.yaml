apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubernetes-dbaas.fullname" . }}
  labels:
    {{- include "kubernetes-dbaas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kubernetes-dbaas.selectorLabels" . | nindent 4 }}
